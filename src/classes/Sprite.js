/*
* CSSImageValue
* HTMLImageElement
* SVGImageElement
* HTMLVideoElement
* HTMLCanvasElement
* ImageBitmap
* OffscreenCanvas
* */

class Sprite {
  constructor({ name, source, width, height, frame: { rate, map, sequence } = {} }) {

  }
}
